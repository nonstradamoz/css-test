{
  "name": "centralised-expense-refund-system",
  "version": "1.0.0",
  "description": "Production-Grade Centralised Expense & Refund Management System (CRS)",
  "private": true,
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:functions": "cd functions && npm run build:watch",
    "dev:emulators": "JAVA_HOME=\"/opt/homebrew/opt/openjdk\" PATH=\"/opt/homebrew/opt/openjdk/bin:$PATH\" firebase emulators:start --project amrita-crs --only auth,firestore,functions",
    "build": "cd functions && npm run build && cd ../frontend && npm run build",
    "seed": "npx ts-node scripts/seed.ts",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "cd functions && npm test",
    "test:integration": "npx vitest run tests/integration.test.ts",
    "lint": "cd frontend && npm run lint && cd ../functions && npm run lint"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
